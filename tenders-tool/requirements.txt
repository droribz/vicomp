# Core
playwright==1.49.1            # browser automation (JS-heavy / login-gated sites)
httpx==0.28.1                # fast direct file downloads
pydantic==2.10.4             # unified data model + validation
PyYAML==6.0.2               # config files (keywords.yaml / sources.yaml)
Jinja2==3.1.5               # report.html rendering
python-dotenv==1.0.1         # load credentials from .env
python-dateutil==2.9.0.post0 # flexible date parsing
tzdata==2024.2              # time zones (Asia/Jerusalem) on Windows
beautifulsoup4==4.12.3       # HTML parsing of list/detail pages (html.parser)

# Dev / tests
pytest==8.3.4
